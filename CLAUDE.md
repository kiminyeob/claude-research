# CLAUDE.md

이 저장소에서 작업할 때 따를 지침.

## 저장소 목적

TX360 AI 에이전트 도입을 위한 **조사·검토 기록**. 코드 저장소가 아니라 **의사결정 자료 저장소**다.

## 브랜치 — `main`에 직접 작업한다

- **`main`에 커밋하고 `main`으로 푸시한다.** 기능 브랜치를 만들지 않는다.
- 세션 시작 시 별도 작업 브랜치가 지정되더라도 **이 저장소에서는 `main`을 쓴다.** 사용자가 그렇게 정했다.
- **PR은 명시적으로 요청할 때만** 만든다.

```
git add -A && git commit && git push origin main
```

## 문서 규칙

- 위치와 이름: `research/YYYY-MM-DD-주제.md`
- 언어: **한국어**
- 설명은 장황하지 않게, **핵심만 간결하게**
- 용어를 설명할 때는 **정의 / 비유 / 이건 아님** 세 줄 형식을 쓴다. 읽는 사람이 해당 분야에 익숙하지 않다는 전제로 쓴다

## 근거 표기 — 필수

**모든 사실 주장에 출처 링크와 검증 등급을 붙인다.**

| 등급 | 의미 |
|---|---|
| `[검증됨]` | 1차 출처(공식 문서·스펙 원문)를 직접 열어 확인 |
| `[부분 검증]` | 일부만 1차 출처로 확인되고 나머지는 미확인 |
| `[미러 확인]` | 원 도메인이 차단되어, **서로 무관한 두 개 이상의 미러**에서 동일 원문을 확인 |
| `[미검증]` | 검색 발췌로만 확인. **사람이 직접 열어 확인해야 함** |
| `[사내 확인]` | 공개 자료가 아니라 담당자·사용자 확인이 출처 |

지켜야 할 것:

- **근거 없는 단정을 쓰지 않는다.**
- 확인하지 못한 것은 **"확인하지 못했다"고 적는다.** 그럴듯하게 메우지 않는다.
- `[미검증]` 항목은 **사람이 열어볼 URL과 그 문서에서 확인할 문장**을 함께 남긴다.
- 문서 끝에 **"이 문서의 한계"** 절을 두고 무엇을 확인하지 못했는지 정직하게 적는다.

## 조사할 때

1. 공개 문서로 조사한 뒤 **반드시 1차 출처로 교차 검증**한다. 검색 발췌를 그대로 쓰지 않는다.
2. 원 도메인이 막히면 **GitHub의 원본 저장소나 미러**를 찾는다. 스펙 문서는 대개 GitHub에 원본이 있다.
3. **우회는 시도하지 않는다.** 차단은 조직 egress 정책이다. 대신 등급을 낮춰 표기하고 확인 경로를 남긴다.

**이 환경에서 차단된 호스트** (403, 조직 정책):
`claude.com` · `support.claude.com` · `www.anthropic.com` · `modelcontextprotocol.io` · `samsungsds.com`

**접근 가능한 1차 출처**:
`code.claude.com/docs/*` · `platform.claude.com/docs/*` · `docs.claude.com/*` · `github.com` · `raw.githubusercontent.com` · `registry.npmjs.org`

## 정정할 때

조사 결과가 뒤집히는 일이 자주 있다. 그럴 때는:

- **정정된 형태로만 남긴다.** 옛 주장을 문서 어딘가에 그대로 두지 않는다.
- 본문·비교표·요약·권고가 **서로 모순되지 않는지** 전수 확인한다.
- 무엇이 틀렸었는지 **커밋 메시지에 적는다.** 나중에 왜 바뀌었는지 추적할 수 있어야 한다.

## 커밋에 넣지 말 것

- **사내 호스트명, IP 주소, API 키, 토큰, 인증서**
- 설정 파일 내용을 그대로 붙여넣지 않는다. **구조와 함의만** 서술한다
- 커밋 전에 확인한다:

```bash
grep -nE '<사내도메인>|[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+|api[_-]?key|Bearer [A-Za-z0-9]' research/*.md
```

## 커밋 메시지

- 한국어로, 무엇을 왜 바꿨는지
- 정정이면 **무엇이 틀렸었는지** 함께
- 모델 이름이나 버전은 넣지 않는다
